#include <stdio.h>

int memoization(int n, int m, int t, int *time_drinking);

int main(void) {
  int n, m, t, num_burgers, time_drinking = 0;

  while (scanf("%d %d %d", &n, &m, &t) != EOF) {
  }

  return 0;
}

int memoization(int n, int m, int t, int *time_drinking) {

  return 0;
}
